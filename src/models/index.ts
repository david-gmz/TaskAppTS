import type {
    InputProps,
    InitState,
    ButtonProps,
    OnStartAdd,
    ProjectProps,
    NewProjectProps
} from "./propsTypes";
import { ActionType } from "./actionTypes";
import type { Action } from "./actionTypes";

export type {
    InputProps,
    InitState,
    ButtonProps,
    OnStartAdd,
    ProjectProps,
    NewProjectProps
};
export { ActionType };
export type { Action };
