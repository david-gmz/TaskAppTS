import type {
    InputProps,
    InitState,
    ButtonProps,
    SidebarProps,
    ProjectProps,
    NewProjectProps
} from "./propsTypes";
import { ActionType } from "./actionTypes";
import type { Action } from "./actionTypes";

export type {
    InputProps,
    InitState,
    ButtonProps,
    SidebarProps,
    ProjectProps,
    NewProjectProps
};
export { ActionType };
export type { Action };
